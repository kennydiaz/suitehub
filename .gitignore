# Dependencies
node_modules/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment
.env
.env.local
.env.*.local

# Editor
.DS_Store
.vscode/
!.vscode/extensions.json
!.vscode/settings.json
.idea/

# OS
Thumbs.db
desktop.ini

# Astro
.astro/

# Build output — regenerado por CI (deploy.yml hace npm run build antes del FTP).
# No se versiona: ensucia diffs y el CI ignora el dist/ commiteado.
dist/

# FTP deploy state (lo gestiona la GitHub Action en el servidor)
.ftp-deploy-sync-state.json
